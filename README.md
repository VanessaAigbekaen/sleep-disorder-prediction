# sleep-disorder-prediction
A machine-learning application that predicts the likelihood of a sleep disorder from sleep, lifestyle and health measurements. The project compares five classification algorithms and integrates the selected Random Forest model into an interactive Python GUI.
